<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- POS SESSION FORM -->
    <record id="view_pos_session_form_inherit_pay_invoice" model="ir.ui.view">
        <field name="name">pos.session.form.inherit.pay.invoice</field>
        <field name="model">pos.session</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_session_form"/>
        <field name="arch" type="xml">

            <xpath expr="//div[@name='button_box']" position="inside">

                <!-- Para que los attrs funcionen de forma fiable -->
                <field name="cash_control" invisible="1"/>

                <!-- Customer Refund (out_refund) -->
                <button
                    class="oe_stat_button"
                    name="button_show_wizard_pay_out_refund"
                    type="object"
                    icon="fa-level-up"
                    attrs="{'invisible':['|',('cash_control','=',False),('state','not in',['opened','closing_control'])]}"
                >
                    <div class="o_form_field o_stat_info">
                        <span class="o_stat_text">Pay</span>
                        <span class="o_stat_text">Refund</span>
                    </div>
                </button>

                <!-- Vendor Invoice (in_invoice) -->
                <button
                    class="oe_stat_button"
                    name="button_show_wizard_pay_in_invoice"
                    type="object"
                    icon="fa-level-up"
                    attrs="{'invisible':['|',('cash_control','=',False),('state','not in',['opened','closing_control'])]}"
                >
                    <div class="o_form_field o_stat_info">
                        <span class="o_stat_text">Pay Supplier</span>
                        <span class="o_stat_text">Invoice</span>
                    </div>
                </button>

                <!-- Customer Invoice (out_invoice) -->
                <button
                    class="oe_stat_button"
                    name="button_show_wizard_pay_out_invoice"
                    type="object"
                    icon="fa-level-down"
                    attrs="{'invisible':['|',('cash_control','=',False),('state','not in',['opened','closing_control'])]}"
                >
                    <div class="o_form_field o_stat_info">
                        <span class="o_stat_text">Get Payment</span>
                        <span class="o_stat_text">from Invoice</span>
                    </div>
                </button>

            </xpath>

        </field>
    </record>

</odoo>
